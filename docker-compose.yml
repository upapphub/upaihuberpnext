# ERPNext + custom fork (this repo) via frappe_docker-style stack.
# Setup: cp .env.example .env && docker compose up -d
# App: http://localhost:8090  (Administrator password: ADMIN_PASSWORD in .env)
# Ops: see DEPLOY.md

x-poddar-python: &poddar-python
  PYTHONPATH: /home/frappe/frappe-bench/apps/poddar_os

services:
  backend:
    image: frappe/erpnext:${ERPNEXT_VERSION:-v16.32.3}
    networks:
      - frappe_network
      - odysseus
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: on-failure
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - .:/home/frappe/frappe-bench/apps/erpnext
      - /home/upoddar/poddar_os:/home/frappe/frappe-bench/apps/poddar_os
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      <<: *poddar-python
    depends_on:
      configurator:
        condition: service_completed_successfully

  configurator:
    image: frappe/erpnext:${ERPNEXT_VERSION:-v16.32.3}
    networks:
      - frappe_network
    restart: "no"
    entrypoint:
      - bash
      - -c
    command:
      - >
        ls -1 apps > sites/apps.txt;
        bench set-config -g db_host $$DB_HOST;
        bench set-config -gp db_port $$DB_PORT;
        bench set-config -g redis_cache "redis://$$REDIS_CACHE";
        bench set-config -g redis_queue "redis://$$REDIS_QUEUE";
        bench set-config -g redis_socketio "redis://$$REDIS_QUEUE";
        bench set-config -gp socketio_port $$SOCKETIO_PORT;
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      REDIS_CACHE: redis-cache:6379
      REDIS_QUEUE: redis-queue:6379
      SOCKETIO_PORT: "9000"
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - .:/home/frappe/frappe-bench/apps/erpnext
      - /home/upoddar/poddar_os:/home/frappe/frappe-bench/apps/poddar_os

  create-site:
    image: frappe/erpnext:${ERPNEXT_VERSION:-v16.32.3}
    networks:
      - frappe_network
    restart: "no"
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - .:/home/frappe/frappe-bench/apps/erpnext
      - /home/upoddar/poddar_os:/home/frappe/frappe-bench/apps/poddar_os
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      SITE_NAME: ${SITE_NAME:-frontend}
    entrypoint:
      - bash
      - -c
    command:
      - |
        wait-for-it -t 120 db:3306
        wait-for-it -t 120 redis-cache:6379
        wait-for-it -t 120 redis-queue:6379
        export start=$$(date +%s)
        until [[ -n "$$(grep -hs ^ sites/common_site_config.json | jq -r '.db_host // empty')" ]] \
          && [[ -n "$$(grep -hs ^ sites/common_site_config.json | jq -r '.redis_cache // empty')" ]] \
          && [[ -n "$$(grep -hs ^ sites/common_site_config.json | jq -r '.redis_queue // empty')" ]]; do
          echo "Waiting for sites/common_site_config.json to be created"
          sleep 5
          if (( $$(date +%s) - start > 120 )); then
            echo "could not find sites/common_site_config.json with required keys"
            exit 1
          fi
        done
        echo "sites/common_site_config.json found"
        if [ -d "sites/$$SITE_NAME" ]; then
          echo "Site $$SITE_NAME already exists, skipping creation"
        else
          bench new-site --mariadb-user-host-login-scope='%' \
            --admin-password="$$ADMIN_PASSWORD" \
            --db-root-username=root \
            --db-root-password="$$MYSQL_ROOT_PASSWORD" \
            --install-app erpnext \
            --install-app poddar_os \
            --set-default $$SITE_NAME $$SITE_NAME
        fi

  db:
    image: mariadb:11.8
    networks:
      - frappe_network
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      start_period: 5s
      interval: 5s
      timeout: 5s
      retries: 5
    restart: on-failure
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --skip-character-set-client-handshake
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - db-data:/var/lib/mysql

  frontend:
    image: frappe/erpnext:${ERPNEXT_VERSION:-v16.32.3}
    networks:
      - frappe_network
    depends_on:
      - websocket
      - backend
    restart: on-failure
    command:
      - nginx-entrypoint.sh
    environment:
      BACKEND: backend:8000
      FRAPPE_SITE_NAME_HEADER: ${FRAPPE_SITE_NAME_HEADER:-frontend}
      SOCKETIO: websocket:9000
      UPSTREAM_REAL_IP_ADDRESS: 127.0.0.1
      UPSTREAM_REAL_IP_HEADER: X-Forwarded-For
      UPSTREAM_REAL_IP_RECURSIVE: "off"
      PROXY_READ_TIMEOUT: 120
      CLIENT_MAX_BODY_SIZE: 50m
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - .:/home/frappe/frappe-bench/apps/erpnext
      - /home/upoddar/poddar_os:/home/frappe/frappe-bench/apps/poddar_os
    ports:
      - "8090:8080"

  queue-long:
    image: frappe/erpnext:${ERPNEXT_VERSION:-v16.32.3}
    networks:
      - frappe_network
      - odysseus
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: on-failure
    command:
      - bench
      - worker
      - --queue
      - long,default,short
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - .:/home/frappe/frappe-bench/apps/erpnext
      - /home/upoddar/poddar_os:/home/frappe/frappe-bench/apps/poddar_os
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379
      <<: *poddar-python
    depends_on:
      configurator:
        condition: service_completed_successfully

  queue-short:
    image: frappe/erpnext:${ERPNEXT_VERSION:-v16.32.3}
    networks:
      - frappe_network
      - odysseus
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: on-failure
    command:
      - bench
      - worker
      - --queue
      - short,default
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - .:/home/frappe/frappe-bench/apps/erpnext
      - /home/upoddar/poddar_os:/home/frappe/frappe-bench/apps/poddar_os
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379
      <<: *poddar-python
    depends_on:
      configurator:
        condition: service_completed_successfully

  redis-queue:
    image: redis:6.2-alpine
    networks:
      - frappe_network
    restart: on-failure
    volumes:
      - redis-queue-data:/data

  redis-cache:
    image: redis:6.2-alpine
    networks:
      - frappe_network
    restart: on-failure

  scheduler:
    image: frappe/erpnext:${ERPNEXT_VERSION:-v16.32.3}
    networks:
      - frappe_network
      - odysseus
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: on-failure
    command:
      - bench
      - schedule
    environment:
      <<: *poddar-python
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - .:/home/frappe/frappe-bench/apps/erpnext
      - /home/upoddar/poddar_os:/home/frappe/frappe-bench/apps/poddar_os
    depends_on:
      configurator:
        condition: service_completed_successfully

  websocket:
    image: frappe/erpnext:${ERPNEXT_VERSION:-v16.32.3}
    networks:
      - frappe_network
    restart: on-failure
    command:
      - node
      - /home/frappe/frappe-bench/apps/frappe/socketio.js
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - .:/home/frappe/frappe-bench/apps/erpnext
      - /home/upoddar/poddar_os:/home/frappe/frappe-bench/apps/poddar_os
    depends_on:
      configurator:
        condition: service_completed_successfully

volumes:
  db-data:
  redis-queue-data:
  sites:
  logs:

networks:
  frappe_network:
    driver: bridge
  odysseus:
    name: upaihubodysseus_default
    external: true
